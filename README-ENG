# Google Apps Script: Classroom Discovery Tool

This script finds your "Classroom" folder automatically, lists your classes, and lets you manage files without ever leaving the spreadsheet.

## Setup Instructions

1.  **Create a New Google Sheet** (e.g., "Classroom Manager").
2.  **Open Extensions > Apps Script**.
3.  **Paste the following code**:

```javascript
// --- CONFIGURATION ---
var ROOT_FOLDER_NAME = "Classroom"; 
// ---------------------

function onOpen() {
  SpreadsheetApp.getUi()
    .createMenu('🎓 Classroom Manager')
    .addItem('1. Find My Classes', 'listClassFolders')
    .addItem('2. Fetch Files from Selected Class', 'fetchFilesFromSelection')
    .addItem('3. Move Files', 'processMoveQueue')
    .addToUi();
}

function listClassFolders() {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  sheet.clear();
  
  sheet.appendRow(["Select", "Class Name", "Folder ID", "Folder URL"]);
  sheet.getRange("A1:D1").setFontWeight("bold");
  
  var folders = DriveApp.getFoldersByName(ROOT_FOLDER_NAME);
  var rootFolder;
  
  if (folders.hasNext()) {
    rootFolder = folders.next();
  } else {
    SpreadsheetApp.getUi().alert("Could not find a folder named '" + ROOT_FOLDER_NAME + "'.");
    return;
  }

  try {
    var subfolders = rootFolder.getFolders();
    var rows = [];
    
    while (subfolders.hasNext()) {
      var folder = subfolders.next();
      rows.push(["☐", folder.getName(), folder.getId(), folder.getUrl()]);
    }
    
    if (rows.length > 0) {
      var range = sheet.getRange(2, 1, rows.length, 4);
      range.setValues(rows);
      sheet.getRange(2, 1, rows.length, 1).insertCheckboxes();
      SpreadsheetApp.getUi().alert("Found " + rows.length + " classes.");
    } else {
      SpreadsheetApp.getUi().alert("Found 'Classroom' folder, but it looks empty.");
    }
  } catch (e) {
    SpreadsheetApp.getUi().alert("Error accessing folders: " + e.message);
  }
}

function fetchFilesFromSelection() {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  var data = sheet.getDataRange().getValues();
  var selectedFolderId = null;
  var selectedClassName = "";

  // Skip header, find checked box
  for (var i = 1; i < data.length; i++) {
    if (data[i][0] === true) { 
      selectedClassName = data[i][1];
      selectedFolderId = data[i][2];
      break; 
    }
  }

  if (!selectedFolderId) {
    SpreadsheetApp.getUi().alert("Please check a box next to a class folder first.");
    return;
  }

  // Clear everything and explicitly remove validations
  sheet.clear();
  sheet.getRange("A:E").clearDataValidations(); 
  
  sheet.appendRow(["Source: " + selectedClassName, "ID: " + selectedFolderId]);
  sheet.appendRow(["File Name", "File Link", "Target Folder URL (Paste Here)", "Status", "File ID"]);
  sheet.getRange("A2:E2").setFontWeight("bold");

  try {
    var folder = DriveApp.getFolderById(selectedFolderId);
    var files = folder.getFiles();
    var allFiles = [];

    // Diagnostics
    var shortcutCount = 0;
    var brokenShortcutCount = 0;

    // Format Regex for sorting: Starts with Year_ (e.g. 2025_...)
    var formatRegex = /^\d{4}_/;

    while (files.hasNext()) {
      var file = files.next();
      var name = file.getName(); // Default: Use the file's own name
      var id = file.getId();
      var url = file.getUrl();
      var isShortcut = false;
      var targetAccessError = false;
      
      // --- SHORTCUT HANDLING ---
      if (file.getMimeType() === "application/vnd.google-apps.shortcut") {
        shortcutCount++;
        try {
          var targetId = file.getTargetId(); // Get Target ID
          
          // Try to get the actual target file object
          try {
            var targetFile = DriveApp.getFileById(targetId);
            url = targetFile.getUrl(); // Use Target URL if accessible
          } catch (accessError) {
             // If we can't access the target object (e.g. permission issues),
             // We fallback to keeping the Shortcut's URL, but we still use Target ID.
             console.log("Could not access target object for: " + name);
             targetAccessError = true;
          }

          // KEY BEHAVIOR: 
          // 1. Keep Shortcut Name (Visual)
          // 2. Use Target ID (Functional)
          // 3. Use Target URL if possible, else Shortcut URL
          name = file.getName(); 
          id = targetId; 
          isShortcut = true;

        } catch (e) {
          // If even getTargetId() fails (very rare), then it's truly broken.
          brokenShortcutCount++;
          console.log("Totally Broken Shortcut: " + file.getName());
          continue; 
        }
      }
      // -------------------------

      if (targetAccessError) {
        name = name + " [⚠️ Restricted Target]";
      }

      allFiles.push({
        name: name,
        url: url,
        id: id,
        isShortcut: isShortcut
      });
    }

    // --- SORTING LOGIC ---
    var formattedFiles = [];
    var otherFiles = [];

    allFiles.forEach(function(item) {
      if (formatRegex.test(item.name)) {
        formattedFiles.push(item);
      } else {
        otherFiles.push(item);
      }
    });

    // Natural Sort for Formatted files
    formattedFiles.sort(function(a, b) {
      return a.name.localeCompare(b.name, undefined, {numeric: true, sensitivity: 'base'});
    });

    // Alphabetical (Japanese) for others
    otherFiles.sort(function(a, b) {
      return a.name.localeCompare(b.name, 'ja');
    });

    var sortedFiles = formattedFiles.concat(otherFiles);
    var rows = [];

    sortedFiles.forEach(function(file) {
      rows.push([file.name, file.url, "", "Pending", file.id]);
    });

    if (rows.length > 0) {
      sheet.getRange(3, 1, rows.length, 5).setValues(rows);
    } else {
      var msg = "No files listed.";
      if (shortcutCount > 0) {
        msg += "\n\nDiagnostic Info:";
        msg += "\n- Shortcuts found: " + shortcutCount;
        msg += "\n- Truly Broken Shortcuts: " + brokenShortcutCount;
      }
      SpreadsheetApp.getUi().alert(msg);
      sheet.appendRow(["No files found.", "", "", "", ""]);
    }
  } catch (e) {
     SpreadsheetApp.getUi().alert("Error fetching files: " + e.message);
  }
}

function processMoveQueue() {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  var lastRow = sheet.getLastRow();
  if (lastRow < 3) return; 

  var dataRange = sheet.getRange(3, 1, lastRow - 2, 5);
  var data = dataRange.getValues();
  var ui = SpreadsheetApp.getUi();
  
  var processedCount = 0;
  var myEmail = Session.getActiveUser().getEmail();

  for (var i = 0; i < data.length; i++) {
    var sheetName = data[i][0]; // The name displayed in the Sheet
    var targetUrl = data[i][2];
    var status = data[i][3];
    var fileId = data[i][4]; // The TARGET ID
    
    // Skip rows that are already Done or Skipped
    if (status === "Done" || status.indexOf("Done") === 0 || status === "Skipped") continue;

    // We process if there is a Target URL provided
    if (targetUrl !== "") {
      var log = [];
      try {
        var file = DriveApp.getFileById(fileId);
        
        // --- SAFETY CHECK: OWNERSHIP ---
        var owner = file.getOwner();
        if (!owner || owner.getEmail() !== myEmail) {
          data[i][3] = "Skipped (Not Owner)";
          continue;
        }

        // 1. MOVE
        var targetFolderId = extractIdFromUrl(targetUrl);
        var targetFolder = DriveApp.getFolderById(targetFolderId);
        file.moveTo(targetFolder);
        log.push("Moved");

        // 2. NAME SYNC (Renaming the Real File to match the Sheet Name)
        // This ensures the Real File adopts the "Clean" name you gave the shortcut.
        if (file.getName() !== sheetName) {
           file.setName(sheetName);
           log.push("Renamed to Match");
        }

        data[i][3] = "Done (" + log.join(" & ") + ")";
        processedCount++;

      } catch (e) {
        data[i][3] = "Error: " + e.message;
      }
    }
  }
  
  // Write back status
  dataRange.setValues(data);
  
  ui.alert('Processed ' + processedCount + ' files.');
}

function extractIdFromUrl(url) {
  if (!url) throw new Error("Empty URL");
  if (url.length < 50 && url.indexOf("http") === -1) return url;
  var match = url.match(/[-\w]{25,}/);
  return match ? match[0] : null;
}
```

4.  **Save** and **Refresh** the Sheet.

## How to Use

1.  **Authorize:** Run any command from the menu. You will see the "Authorization Required" popup. This is safe; it's asking permission to access *your* Drive folders.
2.  **Step 1: Find My Classes:**
    *   Click **🎓 Classroom Manager** > **1. Find My Classes**.
    *   The sheet will clear and list all folders found inside your "Classroom" folder.
3.  **Step 2: Select & Fetch:**
    *   **Check the box** (Column A) next to the class you want to organize.
    *   Click **2. Fetch Files from Selected Class**.
    *   The sheet will reload with a list of all files in that class.
4.  **Step 3: Assign & Move:**
    *   Paste destination Folder URLs into **Column C**.
    *   Click **3. Move Files**.

## Troubleshooting Shortcuts
If the script says "folder not found" or behaves oddly, it's possible your "Classroom" folder is named differently (e.g., "Google Classroom") or is deeply nested.
*   **Fix:** Edit the top line of the code: `var ROOT_FOLDER_NAME = "Classroom";` to match your actual folder name.
